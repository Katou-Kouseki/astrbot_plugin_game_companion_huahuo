# 更新日志

## 0.5.6 - 2026-09-08

### 变更
- 通知到群文案统一为「名字在前、身份在后」格式：`🏆 胜利方：1号张三（平民）、2号李四（平民）`、`💔 失败方：1号王五（卧底）、2号赵六（平民）`。
- 通知到群大字报：获胜玩家名单整体下移，与中央大标题拉开间距（大标题底部到名单约 70px），高度加高避免拥挤。
- 花火复盘收敛攻击性：从「毒舌犀利」调整为「轻松俏皮、善意调侃」，明确不攻击不贬低任何玩家。

## 0.5.5 - 2026-09-08

### 新增
- 谁是卧底「发言 & 投票时间线」自动跟随：新信息出现时自动滚动到底部查看最新内容；仅在玩家本就贴底（在跟随最新消息）时生效，向上翻看历史时不会被强制下拉，不影响手动滑动。
- 发言逐字打字动画期间同样保持贴底跟随，最新一行不会被推出可视区。

## 0.5.4 - 2026-09-08

### 变更
- 谁是卧底分阵营胜场（徽章数据源）改为**按 QQ 号稳定存储**（`qq:xxx`），AI / 未绑定玩家按名字（`name:xxx`）：玩家在 QQ 群修改昵称后重新绑定，胜场与徽章仍跟随 QQ 号，不再因改昵称而丢失。
- 旧版按裸昵称存储的分阵营胜场数据自动迁移（`name:xxx` 前缀），并在首次按 QQ 号记分时合并进对应 QQ，升级后徽章不会回退。
- 徽章查询支持兼容回退：真人按 QQ 键优先，无数据时回退到同名 `name:` 旧数据。

### 修复
- 修复玩家修改 QQ 昵称并重新绑定后，排行榜与徽章数据“消失”的问题（根因：分阵营胜场原先按昵称存储，改昵称后旧数据成为孤儿、新昵称从零开始）。
- 修复全员就绪确认窗口内玩家全部退席时，窗口标记残留导致下次全员就绪被误判“早已到点”而秒开的问题（退席时无条件清除标记）。
- 修复分阵营胜场文件中随键保存的显示名在插件重载时被丢弃的问题（加载时保留 `name` 字段）。

## 0.5.3 - 2026-09-08

### 变更
- 游戏管理台排版优化：全局战绩排行与操作日志表格不再被全局 1100px 最小宽度拖累（按各自列数适配宽度），无需再横向拉滚动条即可看全；房间列表仍保持宽表可横向滚动。
- 编辑战绩改为页面内输入框：管理台运行在沙箱 iframe 中，浏览器会拦截 `window.prompt`，导致「编辑战绩」点击无反应；现改用原生 `<dialog>` 输入框（同样支持 Esc/取消）。
- 管理台删除卧底词条对的确认提示从 `window.confirm` 改为页面内确认对话框（沙箱同样拦截 confirm）。

### 修复
- 修复游戏管理台「编辑战绩」点击无反应（沙箱 iframe 拦截 window.prompt）。
- 修复全局战绩排行、操作日志表格因固定 1100px 最小宽度被迫横向滚动才能看全的问题。

## 0.5.2 - 2026-09-08

### 新增
- PC 分享战报：下载图片的同时把战报图片复制进剪贴板，玩家 Ctrl+V 即可粘贴到聊天窗口；移动端（微信/TIM）仍走系统分享面板。
- 通知到群海报升级为 1080 宽高清，分享战报升级为 2x 高清（iOS 相册里更清晰）。

### 变更
- 通知到群文案优化：词条/胜利方/失败方前加 emoji（📌 / 🏆 / 💔）；失败方列出玩家身份（如 1号张三（卧底））。
- 通知海报排版优化：胜利方名单与大标题拉开间距、最长 3 行自动换行，词条药丸随名单行数动态下移；败方带身份并加 💔 前缀。
- 分享战报美术优化：顶部辉光、右下装饰圆环、整体暗角、底部分隔线与「花火陪你玩」标语。
- 花火复盘改为俏皮风格：按「毒舌解说员花火」人设生成复盘（可带网络梗与 emoji），本地兜底文案同步俏皮化。
- 管理台清空排行榜（含指定「谁是卧底」玩法）时，同步清空分阵营胜场徽章数据，徽章不再残留旧胜场。
- 移动端玩家卡徽章适当缩小，避免占过多行高。

### 修复
- 修复徽章胜场数与战绩排行榜不同步：清空排行榜后徽章仍显示的遗留问题。
- 修复 iOS 分享战报图片偏糊：画布按 2x 输出。

## 0.5.1 - 2026-09-08

### 新增
- 群通报升级为「通知到群」：文案顺序优化为「标题 → 词条 → 胜利方（列出各玩家名）→ 失败方」，不再写惩罚；复盘统一复用同一局缓存内容，并移动到底部图片之后。
- 通知到群新增专属「战况大字报」海报图：与分享战报完全独立的设计（深红横幅式、超大获胜阵营字、醒目词条药丸），随 `undercover.group_announce_style` 配置（仅文字 / 仅图片 / 文字+图片）发送，图片发送失败自动回退纯文字。
- 分享战报改为「屏幕前玩家视角」：按你本局身份随机生成趣味文案（获胜/惜败、平民与队友并肩、卧底演技、白板难得胜利等多款）；观众/未参与对局的玩家显示旁观视角文案，不再误用参战文案；右下角加水印「由 花火 监督生成」。
- 分享战报/通知海报显示每位玩家 QQ 头像（圆形，本人金边高亮；CORS 不可用时回退首字，不污染画布）。
- 徽章展示重做：玩家卡内联显示 emoji 小徽章（移动端也能看到，点按弹出提示窗，桌面悬停显示）；金色荣誉徽章按「累计获胜总局数」描述，阵营徽章才写「该阵营已胜」，并修正 emoji 视觉居中。

### 变更
- 发言轮弹出通知整体缩小（内边距、字号、头像 44→34px 等），排版更紧凑。
- 结算卡「知道了」按钮去掉"查看复盘"字样；「花火复盘」按钮文案固定，不再在生成后变成"查看/刷新"。
- 分享战报保存方式：优先走系统分享面板（iOS 微信/TIM 内可直接存相册/转发），不支持时退化为下载、再退化为新标签打开长按保存。
- 谁是卧底集结阶段若有人退出玩家席（含房主）：自动开局确认窗口立即重置并提示，避免开局倒计时中突然少人仓促开局；剩余玩家保持就绪，重新全员就绪后再开。
- 玩家卡徽章独立成行展示（虚线分隔的「藏品」区），不再与「词条」等身份徽章挤在同一行。

### 修复
- 修复分享战报/通知海报里 QQ 头像一直不显示：qlogo 接口不返回 CORS 头，浏览器无法把跨域头像绘入 Canvas；新增服务端头像代理（`/api/room/{token}/avatar`，仅放行 q1/q2.qlogo.cn）补齐 `Access-Control-Allow-Origin` 后由前端同源加载绘制，拉取失败仍回退首字占位。
- 修复 PK 子轮 AI 不投票卡局：AI 投票兜底分支未按合法目标（PK 只投平票候选人）选取，非法票被服务端拒绝后异常被吞、驱动链断裂；现两处分支都先取合法目标并加兜底。
- 修复分享战报表头与首行文字重叠（数据行未从表头下方起始）。
- 修复迁移配置 schema：新增 `undercover` 配置分组（禁言时长、群通知开关/发送方式、日报/周报等），在插件配置界面正确展示。
- 修复战绩管理「清空所选玩法」：指定玩法即使无数据也不再回退清空全部，避免误清。

## 0.5.0 - 2026-09-07

### 新增
- 「藏品/护身符」徽章分层进阶：卧底/平民/白板三大阵营各有专属阶梯（见习→刺客/守护者/棋士→大师/核心/策士→影帝/之神/幻影，胜场越多点亮越高阶），并按总胜场点亮荣誉段位（新手→常胜→老练→高手→传奇）；徽章显示在玩家卡上，纯装饰不影响对局；分阵营胜场持久化于插件数据目录 `undercover_camp_wins.json`。
- 谁是卧底战绩日报/周报定时推送：改用 AstrBot 自带的 APScheduler（`context.cron_manager.scheduler` + `CronTrigger`，与社区插件一致）注册定时任务，按配置到点把全局卧底战绩榜发到白名单群；不再依赖插件自建轮询。

### 变更
- 群通报与战绩报表的开关/白名单统一沉淀为插件配置项：`undercover.group_announce_enabled`（群通报总开关）、`group_announce_auto`（自动播报）、以及 `report_enabled`、`report_mode`(daily/weekly)、`report_time`(HH:MM)、`report_weekday`(0-6)、`report_groups`（白名单，空格/逗号分隔的 UMO 会话串，如 `UMO:huahuo:GroupMessage:454366619`）。

## 0.4.7 - 2026-09-07

### 新增
- 管理台新增「战绩管理」标签页：按玩法查看全局战绩排行，一键「导出海报」（前端 Canvas 生成 PNG 下载）；每行支持「修正胜场」手动纠偏（纠正代打/统计误差）。
- 管理台操作日志：清空排行榜、修正胜场、关房、安排/移出/踢出玩家、切换玩法等关键动作自动落盘为 JSONL，可随时查看最近 200 条，防止误操作无据可查。
- 谁是卧底结算卡新增「通报到群」：把本局胜负方、词条与（若配置命中）惩罚一键发到开房群；由插件配置 `undercover.group_announce_enabled` 总开关控制（未开启时按钮提示不生效）。

## 0.4.6 - 2026-09-07

### 新增
- 谁是卧底结算卡新增「分享战报」：把本局胜方、词条、全员身份与存活状态一键绘制成 PNG 卡片下载，方便发群炫耀。
- 谁是卧底结算卡新增「生成复盘」：点击时才调用 LLM 生成一段本局复盘摘要（早期出局/可疑发言/胜负关键等），LLM 不可用时回退本地文案，不占用进行中资源。
- AI 头像风格化：AI 玩家的首字占位头像按名字+座号派生 6 档专属底色 + 白色文字，区分更直观；未绑定真人保持中性底色并使用主题强对比文字，深色模式下不再发黑看不清。

### 变更
- QQ 群操作类配置从管理台移出：`undercover.failed_mute_seconds`（失败方禁言）与 `undercover.violated_mute_seconds`（违规说出词条禁言）改为仅在插件配置中设置，管理台「游戏配置」不再展示，管理台只保留游戏房间相关配置。

## 0.4.5 - 2026-09-07

### 新增
- 谁是卧底已绑定 QQ 的玩家展示其 QQ 头像：出现在玩家卡（玩家席网格）、右下座椅面板、发言轮次的头像位置等处直观识别；AI 玩家及未绑定访客以「首字」作为头像占位，无人时显示座号。
- 「轮到 X 号发言」/「到你发言了」弹框头像化：面向队友用头像+「接下来发言 X 号」。谁的卧底主面板与座椅面板的当前浏览者头像同样替换为 QQ 头像。
- 游戏管理台新增「战绩排行榜」管理区块：可一键清空全局战绩排行榜（含房间右侧「本房战绩排行」），带二次确认，避免误触。

### 变更
- 谁是卧底对局进行中（`active`）禁止解绑玩家：前端将「解绑玩家」按钮置灰并提示「对局进行中，结束后才能解绑」，后端 `unbind_identity` 同步拦截，避免中途解绑破坏发言/投票状态。
- 谁是卧底房主更替方案：房主（第一个已绑定 QQ 身份的真人）退席或解绑后，自动由下一位绑定玩家接任；并在房间聊天刷一条系统提示「X号 已成为本房房主」，新接任者同样可在集结界面调整阵营比例或追加 AI 玩家。

### 修复
- 修复 QQ 头像裂图：游戏页面的内容安全策略（CSP）`img-src` 此前只放行 `self` 与 `data:`，`q1.qlogo.cn` 等外部头像域名被浏览器拦截，现已在白名单中加入 `https://q1.qlogo.cn https://q.qlogo.cn`。
- 修复发言/到时通知卡片因头像过大被撑大：通知头像按卡片内文字比例缩放（弹出 44px、顶部倒计时常驻条 24px、紧急态 30px），不再撑大卡片。
- 修复控制台「票数揭晓」报错 `Failed to execute 'add' on 'DOMTokenList'`：当某得票数为 0 时向 `classList.add` 传入了空字符串，改为分开判断后只添加存在的类名。
- 谁是卧底玩家卡优化：已出局玩家的红色叉叉从卡片右上角移到右下角，避开头像与名字；长玩家名（超过卡片宽度）会自动折行、不再溢出或与叉叉重叠。

## 0.4.3 - 2026-09-06

### 新增
- 右栏「游戏规则 + 全局战绩」面板对所有玩法通用：进入哪个游戏就显示哪个游戏的规则与标题（如进入五子棋显示「五子棋游戏规则」）。
- 全局战绩排行榜按游戏类型分别统计并展示：五子棋、中国象棋、井字棋、海龟汤、贪心骰子、你画我猜、二十一点、谁是卧底各自独立排行，互不混算。
- 非卧底游戏获胜时，会把本场已绑定玩家计入对应游戏的全局胜场榜（仅统计已绑定 QQ 的玩家）。

### 变更
- 全局胜场存储结构从单一汇总改为按游戏类型分桶（`game_type -> qq -> 胜场`），旧版卧底数据会自动迁移到「谁是卧底」分桶，历史战绩不丢失。

### 修复
- 最后一轮只剩几名 AI 时不再因兜底文案互相雷同被「相似度拦截」卡死：AI 玩家的发言豁免相似度判定（AI 无法恶意刷屏，且本地兜底模板有限），保证流程总能推进。
- 本地 AI 兜底文案扩充到 8 句/阵营，并按「轮次+座号」偏移选取，同一轮不同 AI 玩家会说出不同的话，不再复读同一句。
- 修复 AI 座位（其内部 QQ 为 `ai-xxx` 随机 token）说出词条违规时触发禁言报错的问题：非数字 QQ 一律跳过群禁言。

## 0.4.2 - 2026-09-06

### 新增
- 发言与投票时间线中，当前正在发言的玩家会显示「我正在思考中…」的动态状态（高亮整条 + 呼吸圆点）；该玩家提交发言后，其文字会以「先慢后快」的方式逐字加速打出。
- 时间线发言文字逐字打出时带光标闪烁效果，让「正在输入」的观感更明显。

### 变更
- 谁是卧底的谈话倒计时警示从 ≤5 秒提前到 ≤30 秒：横幅先放大更醒目，再叠加红色光圈脉冲 + 轻微缩放，催促屏幕前的玩家抓紧发言。
- 房间访问链接的随机码从 32 位字符缩短到约 13 位（`token_urlsafe(10)`），房间链接更简洁，克隆/分享更方便；不影响安全性与唯一性。

### 修复
- 刚开局第一位玩家发言时，若身份卡（花火选词）遮罩正好盖在主界面，「轮到 X 号发言」的横幅在遮罩关闭后也能正常补弹，不再被永久吞掉，首轮发言倒计时不再缺位。
- 修复大量 AI 玩家时对模型供应商的高频/并发请求问题：同一房间同一时刻只允许一个 AI 动作（发言/投票）在途，避免引发 429 限流导致最后一名 AI 发言一直卡住。
- AI 发言兜底提交失败时（如相似度命中、座椅 token 失效）会记录日志并自动跳过该玩家，保证发言/投票轮永不因单个 AI 而整局停滞。
- 右侧面板的「游戏规则 + 全局战绩排行」只在谁是卧底房间展示：其它玩法不再显示卧底专属规则与战绩，避免与当前游戏内容不符。
- 清理了每轮渲染冗余执行的图标刷新（`icons()` 重复调用）。

## 0.4.1 - 2026-09-06

### 新增
- 玩家被淘汰时，屏幕正中出现居中的「离场提示」弹窗，点「知道了」后过渡到本局身份卡。

### 变更
- 「轮到 X 发言」横幅从居中弹出收缩为顶部倒计时常驻条改为平滑滑上缩小，衔接更自然；倒计时 ≤30 秒时横幅放大并加红色脉冲波动提醒玩家。
- 发言超时未被记录的玩家，时间线不再显示「（尚未发言）」，改为「（发言超时，已跳过）」（弱化灰色），且不进入相似度比对。
- 已出局玩家操作区引导不再显示「等待玩家入座」，改为「已出局 · 观战中」。

## 0.4.0 - 2026-09-06

### 新增
- 谁是卧底的「违规禁言 / 失败方禁言」配置已真正生效：说出词条的玩家、以及对局失败方的成员，会在对应 QQ 群里被禁言对应秒数（向 60 秒向上取整）。
- 禁言采用与群管插件一致的 aiocqhttp `bot.set_group_ban`，在创建房间时缓存该群会话的 bot/群号句柄；拿不到或调用失败只记日志，不阻塞游戏。

## 0.3.9 - 2026-09-06

### 修复
- 修复移动端顶栏右上角工具箱图标（外观/主题按钮）因行内 svg 基线产生的偏移，图标现在在按钮内严格居中。
- AI 发言不会再残留 `&&`（含孤立的 `&&`），表情/心情提示词 token 会被彻底清除。
- 投票轮在模型供应商故障（429/断连）时不再永久卡在“投票进行中”：AI 兜底投票只选合法目标（PK 只投平票候选人）、模型超时缩短；投票轮超时后自动给所有未投玩家补投一张本地票。
- 整体 AI 异常时对局也能收尾：AI 发言模型超时缩短并始终有本地兜底文案，发言轮超时直接以本地文案推进；发起/投票均走本地兜底，不再依赖供应商。
- 准备按钮的图标与文字改为居中（进度单独显示在按钮下方），与“加入对局”一致。
- 发言框支持在自己回合按回车提交（Shift+回车换行）。

### 变更
- 发言超时跳过不再生成「本回合发言超时自动跳过」这类占位发言：只推进发言指针，时间线不会残留虚假文案，也不进入相似度比对池。
- 管理台分配玩家路径不再重置谁是卧底的匹配倒计时，避免覆盖开局倒计时。

## 0.3.8 - 2026-09-06

### 新增
- 发言时间线改双列平铺、左右交替；卧底房间隐藏「当前回合」，时间线全宽，身份卡上移。
- 全员准备后进入 2 秒确认窗口再自动开局，期间可取消准备再调整。
- 你画我猜默认视觉模型 Provider 改为 `agnes-ai/agnes-image-2.5-flash`。
- AI 玩家命名为「火花·性格(AI)」（如 火花·调皮(AI)），不显示“号”字，性格形容词按其答题人设随机分配。

### 变更
- 词条泄露检测扩展为「包含词条本身、其倒序或全部字符」（如「刷牙」之于「牙刷」）；投票揭晓优化为「全员+？→ 数字滚动 → 下落砸走问号」，未得票显示 0。
- AI 发言增加阶段性：第 1-2 轮非常笼统抽象、让人无法联想到具体物品，往后再递进具体；整句限制 20 字以内、最多 1 个逗号。
- 被淘汰时给本人更明显的提示，并将淘汰信息通过动画过渡到本局身份卡；全程战绩榜按名字去重（AI 也不再重复显示多条同名）。
- 投票票数只显示数字，去掉“票”字。

### 修复
- 修复「平票 PK 后发言轮被缩减/整局卡死」：PK 子轮不再覆盖固定发言顺序，后续普通轮全员正常发言。
- 修复 PK 投票可选任意人的问题：PK 子轮只能投重新发言的平票候选人；已出局玩家不能再发言或投票。
- 修复「提交发言」按钮在我方回合文字变成同色不可见的问题。
- 修复「告知身份」关闭时 AI 不发言卡局；修复投票揭晓动画因时间线每帧重建被中断的问题（动画节点跨帧缓存）。
- 再来一局不再局限于当前玩家：多人玩家席中的任意玩家都可申请。

## 0.3.7 - 2026-09-06

### 新增
- 本局身份卡移入右侧面板并带飞入动画；违规禁言时长默认改为 300 秒、失败方 60 秒。

### 变更
- 房主阵营比例输入不再被刷新重置；游戏进行中禁止退席。

### 修复
- 关闭「告知身份」后 AI 仍能正常发言/投票（读取对局内部真实身份）。

## 0.3.6 - 2026-09-06

### 新增
- 集结阶段支持「退出对局」回观众席；操作区空档新增状态引导；解绑按钮改纯图标。

### 变更
- 投票票数改「？→数字」揭晓动画；淘汰动画只播一次；准备按钮与开局倒计时增加分隔与间距。

### 修复
- 发言提示不再与「花火选词」身份卡同时弹出。

## 0.3.5 - 2026-09-06

### 新增
- 再来一局回到集结界面并可调整阵营数量；新增玩家「准备」按钮（全员就绪或倒计时自动开局）。
- 时间线新增发言/投票/淘汰过渡动画；房主可随时更改「告知身份」。

### 修复
- 匹配倒计时随加入正确顺延；「告知身份」开关关闭后按预期只发词条。

## 0.3.4 - 2026-09-06

### 新增
- 全局战绩排行：跨房汇总已绑定玩家胜场并持久化；未绑定访客进入绑定引导层。

### 变更
- 匹配倒计时随新玩家加入自动顺延 10 秒。

### 修复
- 管理台操作不再报 500；对局结束后的「再来一局」可正常发起。

## 0.3.3 - 2026-09-06

### 变更
- 发言顺序开局一次性随机后全程固定，白板规避前两席；时间线布局优化。

### 修复
- 刷新后身份/词条卡不再重复弹出。

## 0.3.2 - 2026-09-06

### 新增
- 开局新增「甄选词条」预热动画；发言提醒改为可收缩的顶部横幅。

### 修复
- 深色模式词条卡可读性、AI 命名去重、房间人数统计修正。

## 0.3.1 - 2026-09-05

### 变更
- 更名为「花火陪你玩」，可独立安装运行。

### 修复
- 修复房间绑定引导异常与日志器定义缺失。

## 0.3.0 - 2026-09-04

### 新增
- 新增「谁是卧底」多人玩法：平民/卧底/白板按人数配比、轮流发言投票、平票 PK 与四类胜负判定。
- 支持词库管理/批量导入/LLM 批量生成、AI 补位、房主调整阵营比例、专用 WebUI（开局倒计时、发言提醒、身份词卡与投票界面）及深色主题。

## 0.2.8 - 2026-08-21

### 修复

- 兼容 AstrBot 4.27.3 的插件配置 schema，将日志等级配置改为受支持的 `string` 类型，修复插件安装时因 `select` 类型不受支持而加载失败的问题。

## 0.2.7 - 2026-08-20

### 修复与改进

- 支持配置 FRP、ngrok、Tailscale Funnel 或反向代理提供的外部访问地址，不再强制依赖 Cloudflare。

- 修复 `0.0.0.0` 监听时移动端链接错误指向 `127.0.0.1` 的问题，并支持配置局域网访问地址。

- 支持手动指定 `cloudflared` 路径，游戏管理台可通过代理下载官方二进制到插件数据目录。

- 增加移动端访问状态、隧道路径、来源和健康检查日志；插件日志等级默认跟随 AstrBot 全局设置。

## 0.2.6 - 2026-08-15

### 修复

- Cloudflare Quick Tunnel 现在会在公网健康检查连续通过后才签发游戏房间链接，避免手机立即打开尚未完成 DNS 生效的临时域名。

- 活动房间使用的临时域名连续失联时会被标记为不可用并自动重建，不再因 `cloudflared` 子进程仍存活而长期复用失效链接。

- Quick Tunnel 子进程在 Windows 下改为无窗口启动；通道测试不再依赖仅适用于 Unix 的 shell 脚本。

## 0.2.5 - 2026-08-13

### 新增

- 新增二十一点：Bot 担任庄家，用户作为闲家比点数；支持要牌、停牌和投降，庄家按规则补牌后统一结算。

- 默认仅 1 名玩家与 Bot 单独对局；管理台可把最大玩家席调整为 2-6 人，多人时每位玩家各持一手牌按席位轮流操作。

- 牌堆每局使用六副牌重新洗牌，支持 A 计 1 或 11、自然 21 点、庄家暗牌查牌；easy/normal 庄家软 17 停牌，hard 庄家软 17 补牌。

- WebUI 新增牌桌视图，显示庄家明暗牌、各家手牌点数、回合倒计时和牌局历史；观众看不到未翻开的庄家暗牌。

### 变更

- 二十一点不设筹码，输赢按“赢、输、平局”计入房间比分，并沿用现有记忆和陪伴情绪余韵流程。

- 多人局进行中不允许中途入席，玩家离开或超时会把当前手牌记为停牌/投降，避免牌局卡死。

### 修复

- 二十一点回合倒计时现在会从发牌、要牌和停牌后正确启动，超时自动停牌随之生效。

- 多人局中已离线的未结算玩家会被自动停牌并跳过，不会再阻塞庄家补牌和最终结算。

- 起手自然 21 点的手牌会直接跳过；所有闲家都是自然 21 点时立即进入庄家结算，不再空等超时。

- 管理台修改二十一点最大玩家席后立即生效，不再需要重载插件。

- 多人局由首位入席玩家先行动；某位玩家行动后若其余未完成手牌都已离线结算，会立即进入庄家回合，不再等待看门狗周期。

## 0.2.4 - 2026-08-11

### 变更

- “允许非游戏管理员在群聊创建房间”关闭时保持原规则：只有游戏管理员可以创建，且创建结果固定为管理员房间。

- 开启该选项后，普通成员和游戏管理员默认都创建普通房间；游戏管理员只有在自然语言中明确要求时才创建管理员房间。

- 非游戏管理员不能通过自然语言请求越权创建管理员房间。

## 0.2.3 - 2026-08-11

### 新增

- 五子棋、中国象棋、井字棋、海龟汤、贪心骰子和你画我猜新增独立启用开关，默认全部开启。

- 游戏管理台新增“游戏配置”页面，可视化修改每个游戏的开关、海龟汤多人参数、贪心骰子目标分数、你画我猜参数和象棋引擎下载策略。

- 贪心骰子获胜目标分数支持配置为 20-200，默认 50 分。

### 变更

- 关闭游戏后会阻止新建房间、切换游戏、开始新局和申请再来一局；已经进行或暂停的对局不会被强制结束。

- 管理台保存的设置直接写入 AstrBot 原有插件配置，并立即用于后续房间和新一局，无需维护第二份配置文件。

## 0.2.2 - 2026-08-10

### 修复

- 你画我猜的开场、终局和人格化回复现在始终明确“用户作画、Bot 猜图”，防止 Bot 把猜图结果误说成自己的画技。

- 画布同步改为串行队列；连续快速落笔时不会丢弃后续同步，也不会再由较旧的轮询快照覆盖尚未提交的本地笔迹。

- 点击“让 Bot 猜”前会等待全部待提交笔迹同步完成，确保视觉模型看到最新画面。

## 0.2.1 - 2026-08-06

### 新增

- 新增默认开启的私聊 QQ 游戏事实衔接。对应的单玩家可以在 QQ 中询问当前游戏、比分、执子、轮次和最近结果；注入内容仅用于当前模型请求，不写入普通聊天历史或长期记忆。

- 最近一局结果在房间关闭后默认于内存中保留 30 分钟，可配置为 0-1440 分钟；`0` 表示关闭后立即删除，插件重载后不恢复。

### 修复

- 五子棋随机执子、中国象棋随机执方和井字棋随机标记的开场评论现在明确使用服务端实际结果，并写明黑方、红方或 X 固定先手，避免 Bot 说反颜色与先后手。

- 贪心骰子开场评论现在使用服务端已经确定的随机先手，不再让模型自行猜测。

- 群聊、多人私聊、身份不匹配的私聊继续完全隔离；QQ 只获得结构化只读事实，不包含 WebUI 对话、海龟汤隐藏内容或游戏控制权限。

## 0.2.0 - 2026-08-06

### 修复

- WebUI 房间聊天、游戏事件评论、海龟汤生成与反向玩法、悔棋及再来一局决策现在按当前 AstrBot 会话解析人格，不再固定使用全局默认人格。

- 人格选择与 AstrBot 正常聊天保持相同优先级：会话强制人格、当前对话绑定人格、UMO 路由默认人格；同时保留旧版 AstrBot 的兼容回退。

- 五子棋关键节点评论改为只分析刚完成的落子，不再把整盘候选位置的潜力误称为刚形成的威胁。

- 五子棋评论上下文现在包含落子方、黑白棋、落子坐标、双方棋子数、当前回合，以及对手能否在下一手封住；模型提示使用自然局势描述，不注入专业棋型名称。

## 0.1.10 - 2026-08-06

### 新增

- 新增固定 HTTPS 域名下的“受信任浏览器”可选功能。用户首次通过 QQ 一次性令牌确认后，可选择在同一浏览器中保留身份，新房间无需重复绑定。

- 新增 `/game 撤销网页绑定` 指令，可撤销当前 QQ 的全部受信任浏览器；WebUI 也可单独取消当前浏览器的信任。

- 受信任设备默认有效 30 天，可配置为 1-365 天；每个 QQ 最多保留 8 台设备。

### 安全

- 服务端只落盘设备令牌的 SHA-256 摘要，原始令牌仅保存在 `HttpOnly`、`Secure`、`SameSite=Lax` Cookie 中。

- 长期凭证只恢复 QQ 身份，不自动进入玩家席，也不持久化或恢复房间、棋局和对话；临时隧道继续使用每房间一次性绑定。

- 取消或通过 QQ 撤销信任后不会因当前房间仍已确认身份而自动重新签发凭证。

## 0.1.9 - 2026-08-05

### 新增

- 新增“你画我猜”：用户在 WebUI 画布作画，Bot 通过视觉模型猜测本地词库中的答案。

- 支持简单、普通、困难三档词库；默认 120 秒作画、最多 5 次猜测，均可在插件配置中调整。

- 画笔、颜色、粗细、撤销、清空和手动“让 Bot 猜”均在网页完成；笔迹实时同步给房间观众，但隐藏答案只提供给已绑定玩家。

- 新增视觉模型 Provider 配置；留空时使用当前会话模型，不支持图片时给出明确错误并保留当前画布。

### 变更

- 你画我猜按合作结果记录：猜中和未猜中分别计入房间摘要，不计入 Bot 胜负余韵连败；不调用模型分析每一笔，只在手动猜测时发送一张压缩画布图片。

## 0.1.8 - 2026-08-05

### 新增

- 新增“我会永远陪着你”可选联动：每局结束后只按实际进入过玩家席的已绑定 QQ 上报结构化结果，由陪伴人格分别判断对胜负和共同参与的在意程度。

- 游戏胜负可形成独立的短期情绪余韵；连胜连败允许叠加，但强度上限、持续时间、后续语气及再次邀请意愿均由陪伴人格决定。

- 新增可选主动游戏邀请能力。开启后，陪伴插件可结合人格、日程、关系、免打扰、房间容量和近期游戏余韵，低频邀请用户玩游戏。

### 变更

- 终局余韵在后台结算，不延迟最后一步的 WebUI 状态更新；用户申请“再来一局”时会等待当前余韵完成，再结合申请语气决定保留、缩短、延长或清除。

- 游戏余韵不复用陪伴插件的受伤、拒绝或关系扣分状态；正常输赢不会被解释为用户伤害 Bot。

- 联动默认关闭，陪伴插件未安装或版本不支持时自动降级，既有游戏、记忆和 WebUI 流程保持不变。

## 0.1.7 - 2026-08-05

### 新增

- WebUI 新增独立 Bot 对话区；桌面端与游戏区并列，手机端可在游戏和对话视图间切换。

- 匿名观众、已绑定观众和玩家均可在房间中发言，消息显示昵称与稳定访客序号。

- 支持在房间对话中自然提出悔棋、暂停、继续、投降、再来一局、切换游戏及海龟汤问答。

### 变更

- 原“局内回应”并入 Bot 对话时间线，游戏回复不再发送到 QQ；QQ 仅保留创建房间、发送链接、身份绑定和游戏菜单。

- QQ 普通对话不再注入活动房间状态，WebUI 对话使用独立房间上下文，避免影响群内其他人的正常聊天。

- 游戏指令由服务端按席位校验；观众可以普通聊天，但所有游戏控制请求都会提示权限不足。

- 匿名观众不读取或写入长期记忆；已绑定观众和玩家的对话在房间结束时按 QQ 分开生成有限记忆摘录。

## 0.1.6 - 2026-08-05

### 新增

- 普通房间新增一次性 QQ 绑定令牌：访客可在原群聊或私聊中直接发送令牌，或发送“绑定玩家 令牌”，之后才能进入玩家席。

- 绑定时保存 QQ 昵称/群名片，WebUI、Bot 回合提示和管理台优先显示昵称，并保留稳定访客序号用于重名区分和管理操作。

- 席位身份跟随浏览器访客自身；交换席位时不会转移原玩家的 QQ 身份。

### 修复

- 普通房间不再默认将首位访客视为创建者，避免未确认的临时 QQ 身份参与操作或写入记忆。

## 0.1.5 - 2026-08-04

### 新增

- 海龟汤支持最多 6 名玩家同时进入玩家席并按访客序号轮流操作；席位上限可配置，`0` 表示不限制。

- 新增服务端回合倒计时，默认 60 秒；超时自动跳过至下一位在线玩家，模型处理期间暂停，`0` 可关闭。

- 观众可以向指定玩家发送限频交换申请，目标玩家可接受或拒绝；默认冷却 30 秒、有效期 20 秒。

- 新增“玩家出题、Bot 猜”玩法，由 QQ 自然语言选择。玩家只提交公开线索与回答，不上传隐藏汤底；Bot 猜中后由当前玩家确认。

- 游戏管理台支持为海龟汤连续安排多个玩家、分别绑定 QQ，并可单独将任一玩家移回观众席。

- 新增可复用的房间级多人席位、轮次与交换申请模型，为后续多人游戏保留统一扩展接口。

### 变更

- 普通海龟汤中的提问、完整猜测和提示申请各消耗一个玩家回合；新玩家可在对局进行中加入空余席位。

- 玩家出题模式的 Bot 推理只读取公开回合与人格设定，不读取隐藏答案，也不把逐回合内容写入 AstrBot 普通聊天历史。

- 管理员房间仍禁止访客自行占据玩家席；普通房间允许访客自行加入海龟汤空席。

- 接受交换申请只替换浏览器席位，不转移原玩家绑定的 QQ 身份；交换当前回合席位时重置完整倒计时。

### 修复

- 保留五子棋、中国象棋、井字棋和贪心骰子的单玩家席位契约，切出海龟汤时只保留主玩家，避免多人状态影响既有游戏。

## 0.1.4 - 2026-08-03

### 新增

- 新增贪心骰子：玩家与 Bot 轮流掷一颗六面骰，可继续冒险或收手存分，率先达到 50 分获胜。

- 贪心骰子每局随机先手；Bot 采用稳健、均衡或大胆的基础风险倾向，并根据双方比分和胜利距离动态调整收手阈值。

- WebUI 新增服务端权威的骰子界面、逐次掷骰动画、本回合暂存分、双方总分、操作记录及继续掷/收手操作。

- 游戏管理台增加贪心骰子切换项与实时比分摘要。

- 新增 `/game 游戏菜单` 指令，列出插件全部游戏，并显示全局群聊与私聊房间的当前数量、上限和创建开关状态。

### 变更

- 普通掷骰和 Bot 决策均在本地完成，不调用模型；连续冒险、高分爆掉、大额存分和终局等关键节点继续接入陪伴人格与长期记忆链路。

- 游戏关键节点的快速回复不再写入 AstrBot 普通对话历史，避免未经过陪伴插件完整处理的临时语气影响后续聊天；房间结束后的结构化共同经历总结继续保留。

- 正常 QQ 对话临时注入实时比分、行动方、可靠的局面摘要和当前游戏最近两条快速回复，使 Bot 能承接游戏话题与自己刚说过的话；临时内容不进入普通对话历史或长期记忆。

- 贪心骰子的随机点数只由服务端安全随机源生成，浏览器仅提交“继续掷”或“收手”，不能指定结果。

- 贪心骰子复用房间权限、席位审核、暂停、投降、再来一局、切换游戏和自动销毁机制，并拥有独立累计比分。

### 修复

- 海龟汤模式下明确隐藏无意义的先后手选择模块，避免组件样式覆盖 `hidden` 属性后意外显示。

## 0.1.3 - 2026-08-03

### 新增

- 新增海龟汤，可通过 QQ 自然语言创建或在原房间中切换；管理台同步增加海龟汤切换项。

- Bot 默认实时生成原创汤面、固定汤底、关键事实和渐进提示，并执行第二轮隐藏自洽性校验；连续失败时使用内置安全题兜底。

- QQ 与 WebUI 均可提出是非问题、提交完整推理和申请提示，公开问答记录双向同步。

- WebUI 新增海龟汤专用汤面、问答时间线、提示、答案提交、解谜进度和终局汤底界面。

- 新增全年龄、普通和不限制三档内容等级，以及每题最大提示次数配置。

- 海龟汤独立记录成功解谜、放弃、提问和提示统计；再来一局会在原房间生成全新题目。

### 变更

- 海龟汤普通问答仅进入隐藏裁判链路，不逐条触发陪伴人格和长期记忆；关键线索、提示、终局与再来一局仍正常接入陪伴体验。

- 活跃题目的公开快照和管理台均不暴露汤底或关键事实；题目生成不读取用户私人记忆。

- 出题期间暂停无操作销毁计时，生成或裁判失败不会计入有效提问，玩家可保留原输入重试。

- 海龟汤不提供悔棋；放弃后揭晓固定汤底。

### 修复

- 新游戏逻辑使用独立状态与接口分支，避免影响五子棋、中国象棋和井字棋的落子、引擎、比分及再来一局流程。

## 0.1.2 - 2026-08-02

### 新增

- 新增井字棋，可通过 QQ 自然语言创建房间，或在原房间中从五子棋、中国象棋切换进入。

- 井字棋支持玩家执 X、执 O 或随机；X 方先行，Bot 仍由人格决定简单、普通或困难棋力。

- 简单棋力会保留明显失误概率，普通棋力会主动取胜和阻挡，困难棋力使用完整极小化极大搜索并保持不可被强制击败。

- WebUI 新增响应式 3×3 棋盘、X/O 差异化显示、最后一步高亮和玩家落子的即时反馈。

- 游戏管理台新增井字棋切换项；井字棋拥有独立累计比分，并接入共同经历摘要。

### 变更

- 三种游戏继续共用同一房间权限、访客身份、再来一局、悔棋、暂停、认输和自动销毁机制。

- 井字棋普通落子不触发模型，只在开局、双重威胁、胜负和再来一局等关键节点使用人格化表达。

- 最低 AstrBot 版本调整为 `4.24.2`；该版本已提供游戏管理台依赖的插件自定义页面能力。

## 0.1.1 - 2026-08-02

### 新增

- 新增中国象棋，支持玩家执红、执黑或随机，红方先行。

- 新增 Pikafish 引擎服务，由服务端校验合法着法、判断终局并提供三档 Bot 棋力。

- 游戏管理台新增 Pikafish 状态、按需安装和更新入口；支持配置自备引擎路径及下载代理。

- 支持通过 QQ 自然语言在五子棋和中国象棋之间切换；管理台也可切换房间游戏。

- 象棋 WebUI 新增 9×10 棋盘、楚河汉界、执黑视角翻转、选子和合法落点提示。

- 五子棋与中国象棋分别累计比分，并在房间结束时分别写入共同经历摘要。

### 变更

- 切换游戏时保留原房间链接、玩家身份、访客序号和房间权限。

- 进行中或暂停中的对局必须明确确认放弃后才能切换游戏。

- “再来一局”继续当前游戏类型，不关闭房间或创建新链接。

- 普通走棋不触发模型；象棋仅在重要吃子等关键节点触发人格化回应。

### 修复

- 象棋引擎异常时自动暂停当前象棋对局，不影响五子棋和 AstrBot 其他功能。

- Pikafish 托管更新加入 SHA-256 校验、安装大小限制、原子替换及启动失败回滚。

- 修复 AstrBot 插件页禁止原生模态框时，安装引擎、切换游戏、关闭房间和移出访客点击无响应的问题。

- 房间链接改由插件独立发送为纯文字消息，避免陪伴插件二次改写或 TTS 转换时丢失；发送失败时仍回退到模型回复。

## 0.1.0 - 2026-08-02

### 新增

- 发布首个可用版本，提供五子棋 WebUI、三档人格化棋力和轻微 Bot 落子延迟。

- 支持群聊和私聊房间、来源级并行数量限制及无人/无操作自动销毁。

- 支持普通房间与管理员审核房间，提供访客编号、玩家席安排和抢占纠正。

- 新增 AstrBot 游戏管理台，用于查看房间、安排玩家、暂停、继续和关闭房间。

- 接入现有陪伴人格、生活场景、对话记录和长期记忆链路。

- 支持固定 HTTPS 地址和 Cloudflare Quick Tunnel，并在首选端口冲突时尝试后续端口。

### 修复

- 玩家落子在等待 Bot 应手前立即显示。

- 房间过期链接展示明确的关闭原因。

- 补充插件仓库地址，使 AstrBot 强制更新功能能够定位更新源。

- QQ 中自然语言提出“再来一局”时复用原房间。

- 本局结束后玩家关闭页面可提前销毁房间，并保留心跳超时兜底。


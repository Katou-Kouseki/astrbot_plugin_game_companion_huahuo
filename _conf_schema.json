{
  "server": {
    "description": "游戏页面服务",
    "type": "object",
    "hint": "服务按需启动，默认只监听本机。不会自动开放防火墙或暴露公网 IP。",
    "items": {
      "enabled": {
        "description": "启用游戏页面服务",
        "type": "bool",
        "default": true
      },
      "host": {
        "description": "监听地址",
        "type": "string",
        "default": "127.0.0.1",
        "hint": "推荐保持 127.0.0.1，由 Quick Tunnel 或 HTTPS 反向代理提供外部访问。"
      },
      "access_host": {
        "description": "局域网访问地址",
        "type": "string",
        "default": "",
        "hint": "监听 0.0.0.0 时填写手机实际可访问的局域网 IP；留空时自动探测。"
      },
      "port": {
        "description": "首选监听端口",
        "type": "int",
        "default": 6331,
        "hint": "冲突时只尝试后续十个端口，不会占用或关闭已有服务。"
      },
      "public_base_url": {
        "description": "固定 HTTPS 外部地址",
        "type": "string",
        "default": "",
        "hint": "例如 https://bot.example.com/games。留空时可按需启动 Cloudflare Quick Tunnel。"
      },
      "external_base_url": {
        "description": "其他远程访问地址",
        "type": "string",
        "default": "",
        "hint": "填写 FRP、ngrok、Tailscale Funnel 或反向代理提供的完整 HTTP/HTTPS 地址，优先于 Cloudflare。"
      },
      "auto_quick_tunnel": {
        "description": "创建房间时自动启动临时公网访问",
        "type": "bool",
        "default": true,
        "hint": "没有配置其他外部地址时，按需启动 Cloudflare Quick Tunnel。"
      },
      "cloudflared_path": {
        "description": "cloudflared 可执行文件路径",
        "type": "string",
        "default": "",
        "hint": "留空时按系统 PATH、插件目录和插件托管目录依次查找。"
      },
      "allow_cloudflared_download": {
        "description": "允许管理台下载 cloudflared",
        "type": "bool",
        "default": true,
        "hint": "从 Cloudflare 官方发行版下载到插件数据目录。"
      },
      "cloudflared_download_proxy": {
        "description": "cloudflared 下载代理",
        "type": "string",
        "default": "",
        "hint": "例如 http://127.0.0.1:7890；留空时继承 HTTPS_PROXY 或 HTTP_PROXY。"
      }
    }
  },
  "logging": {
    "description": "插件日志",
    "type": "object",
    "items": {
      "level": {
        "description": "日志等级",
        "type": "string",
        "default": "inherit",
        "hint": "默认跟随 AstrBot 全局日志等级；可填写 inherit、debug、info、warning 或 error，自定义等级只影响本插件。"
      }
    }
  },
  "identity": {
    "description": "网页身份绑定",
    "type": "object",
    "hint": "受信任浏览器只在配置固定 HTTPS 外部地址时生效；临时隧道仍使用每房间一次性令牌。",
    "items": {
      "enable_trusted_browser": {
        "description": "允许记住浏览器身份",
        "type": "bool",
        "default": false,
        "hint": "开启后，用户首次通过 QQ 令牌确认时可选择记住当前浏览器。不会自动加入玩家席或恢复房间。"
      },
      "trusted_browser_ttl_days": {
        "description": "受信任浏览器有效天数",
        "type": "int",
        "default": 30,
        "hint": "范围 1-365；过期后需要重新通过 QQ 令牌绑定。每个 QQ 最多保留 8 台设备。"
      }
    }
  },
  "context": {
    "description": "QQ 游戏事实衔接",
    "type": "object",
    "hint": "只向对应的私聊单玩家临时提供只读游戏事实；不注入 WebUI 对话，不写入普通聊天历史，群聊始终隔离。",
    "items": {
      "enable_private_qq_game_context": {
        "description": "私聊中提供游戏事实",
        "type": "bool",
        "default": true,
        "hint": "让 Bot 在私聊中回答当前游戏、比分、执子和最近结果；所有游戏操作仍只能在 WebUI 完成。"
      },
      "recent_game_result_ttl_minutes": {
        "description": "房间关闭后保留最近结果分钟数",
        "type": "int",
        "default": 30,
        "hint": "范围 0-1440；0 表示房间关闭后立即删除。结果只保存在内存中，插件重载后不恢复。"
      }
    }
  },
  "rooms": {
    "description": "房间权限与并行数量",
    "type": "object",
    "items": {
      "enable_group_rooms": {
        "description": "允许群聊创建房间",
        "type": "bool",
        "default": true
      },
      "enable_private_rooms": {
        "description": "允许私聊创建房间",
        "type": "bool",
        "default": true
      },
      "allow_non_admin_group_creation": {
        "description": "允许非游戏管理员在群聊创建房间",
        "type": "bool",
        "default": false,
        "hint": "关闭时仅管理员可创建，且固定为管理员房间；开启后所有人默认创建普通房间，管理员明确提出时才创建管理员房间。"
      },
      "game_admin_qq_ids": {
        "description": "游戏管理员 QQ 号",
        "type": "text",
        "default": "",
        "hint": "每行一个或使用逗号分隔。不继承 AstrBot 管理员。"
      },
      "max_group_rooms": {
        "description": "全部群聊合计最大房间数",
        "type": "int",
        "default": 1,
        "hint": "0 表示无限制；创建后即占用名额。"
      },
      "max_private_rooms": {
        "description": "全部私聊合计最大房间数",
        "type": "int",
        "default": 1,
        "hint": "0 表示无限制；创建后即占用名额。"
      },
      "empty_player_timeout_seconds": {
        "description": "玩家席无人自动销毁秒数",
        "type": "int",
        "default": 60,
        "hint": "0 表示关闭该项自动销毁。"
      },
      "idle_timeout_seconds": {
        "description": "房间无有效操作自动销毁秒数",
        "type": "int",
        "default": 300,
        "hint": "0 表示关闭该项自动销毁；页面刷新和心跳不算有效操作。"
      }
    }
  },
  "game": {
    "description": "局内节奏",
    "type": "object",
    "items": {
      "commentary_cooldown_seconds": {
        "description": "战术节点人格回复冷却秒数",
        "type": "int",
        "default": 45,
        "hint": "普通落子和掷骰不触发模型，只在明显威胁、连续冒险或高分爆掉等关键节点表达。"
      }
    }
  },
  "multiplayer": {
    "description": "多人席位",
    "type": "object",
    "hint": "当前由海龟汤使用；席位、轮换和交换协议也可供后续多人游戏复用。",
    "items": {
      "turn_timeout_seconds": {
        "description": "每位玩家回合秒数",
        "type": "int",
        "default": 60,
        "hint": "0 表示关闭回合倒计时；模型处理期间自动暂停。"
      },
      "swap_request_cooldown_seconds": {
        "description": "观众交换申请冷却秒数",
        "type": "int",
        "default": 30,
        "hint": "按申请者计时；0 表示不限制发送频率，但仍只能有一条待处理申请。"
      },
      "swap_request_expiry_seconds": {
        "description": "交换申请有效秒数",
        "type": "int",
        "default": 20,
        "hint": "过期申请由服务端自动清理。"
      }
    }
  },
  "gomoku": {
    "description": "五子棋",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用五子棋",
        "type": "bool",
        "default": true
      }
    }
  },
  "xiangqi": {
    "description": "中国象棋引擎",
    "type": "object",
    "hint": "象棋使用独立 Pikafish 进程。引擎不可用时只禁用象棋，不影响五子棋。",
    "items": {
      "enabled": {
        "description": "启用中国象棋",
        "type": "bool",
        "default": true
      },
      "engine_path": {
        "description": "Pikafish 可执行文件路径",
        "type": "string",
        "default": "",
        "hint": "留空时使用游戏管理台安装的托管版本。可执行文件同目录必须包含 pikafish.nnue。"
      },
      "allow_engine_download": {
        "description": "允许管理台下载 Pikafish",
        "type": "bool",
        "default": true
      },
      "auto_download_engine": {
        "description": "首次创建象棋房间时自动下载",
        "type": "bool",
        "default": false,
        "hint": "默认关闭。推荐在游戏管理台确认后按需安装。"
      },
      "download_proxy": {
        "description": "Pikafish 下载代理",
        "type": "string",
        "default": "",
        "hint": "例如 http://127.0.0.1:7890；留空时继承 HTTPS_PROXY 或 HTTP_PROXY。"
      }
    }
  },
  "tictactoe": {
    "description": "井字棋",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用井字棋",
        "type": "bool",
        "default": true
      }
    }
  },
  "turtle_soup": {
    "description": "海龟汤",
    "type": "object",
    "hint": "题目默认由当前会话的 Bot 生成并进行隐藏校验；模型不可用时使用内置兜底题。",
    "items": {
      "enabled": {
        "description": "启用海龟汤",
        "type": "bool",
        "default": true
      },
      "max_hints": {
        "description": "每题最多提示次数",
        "type": "int",
        "default": 3,
        "hint": "范围 0-8；0 表示不额外限制，可依次查看该题全部预生成提示。"
      },
      "content_level": {
        "description": "题目内容等级",
        "type": "string",
        "default": "normal",
        "options": ["all_ages", "normal", "unrestricted"],
        "labels": ["全年龄", "普通", "不限制"],
        "hint": "all_ages 为全年龄，normal 为普通悬疑，unrestricted 允许更沉重题材但仍遵守安全限制。"
      },
      "max_players": {
        "description": "海龟汤最大玩家席数",
        "type": "int",
        "default": 6,
        "hint": "0 表示不限制；普通房间可自行加入空席，管理员房间仍由游戏管理台安排。"
      },
      "free_questioning": {
        "description": "自由提问（不强制轮次）",
        "type": "bool",
        "default": false,
        "hint": "开启后海龟汤常规玩法（Bot 出题）不再要求按玩家席顺位轮流提问，任意玩家席成员都能随时提问/答题；观众仍需先加入玩家席。"
      },
      "free_question_cooldown": {
        "description": "自由提问判定冷却（秒）",
        "type": "int",
        "default": 1,
        "hint": "范围 0-30；限制两次 LLM 判定之间的最短间隔，防止玩家连续提问把模型打满。0 表示不额外限速（仍受单格判断锁约束）。"
      }
    }
  },
  "pig_dice": {
    "description": "贪心骰子",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用贪心骰子",
        "type": "bool",
        "default": true
      },
      "target_score": {
        "description": "获胜目标分数",
        "type": "int",
        "default": 50,
        "hint": "范围 20-200；新一局中先达到目标分数的一方获胜。"
      }
    }
  },
  "draw_guess": {
    "description": "你画我猜",
    "type": "object",
    "hint": "用户在 WebUI 画画，Bot 只在点击猜测时调用一次视觉模型；答案由服务端隐藏保存。",
    "items": {
      "enabled": {
        "description": "启用你画我猜",
        "type": "bool",
        "default": true
      },
      "vision_provider_id": {
        "description": "视觉模型 Provider",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "可选。留空使用当前会话模型；当前模型不支持图片时，请选择支持图片输入的 Provider。"
      },
      "duration_seconds": {
        "description": "作画倒计时秒数",
        "type": "int",
        "default": 120,
        "hint": "范围 10-600；倒计时结束后本轮自动结算。"
      },
      "max_guesses": {
        "description": "Bot 最大猜测次数",
        "type": "int",
        "default": 5,
        "hint": "范围 1-10；每次点击“让 Bot 猜”消耗一次。"
      }
    }
  },
  "blackjack": {
    "description": "二十一点",
    "type": "object",
    "hint": "Bot 担任庄家。默认只允许一名用户单独对局，开放多人后每位玩家各持一手牌依次操作。",
    "items": {
      "enabled": {
        "description": "启用二十一点",
        "type": "bool",
        "default": true
      },
      "max_players": {
        "description": "最大玩家席",
        "type": "int",
        "default": 1,
        "hint": "范围 1-6；1 表示用户只能单独和 Bot 对局，大于 1 时允许多人各自对庄。"
      }
    }
  },
  "undercover": {
    "description": "谁是卧底（对局参数与群播报）",
    "type": "object",
    "hint": "对局参数、群操作与定时播报均在此设置；对局参数也可在游戏管理台「游戏配置」中调整。",
    "items": {
      "enabled": {
        "description": "启用谁是卧底",
        "type": "bool",
        "default": true
      },
      "max_players": {
        "description": "最大玩家席",
        "type": "int",
        "default": 10,
        "hint": "范围 2-20，默认 10 人；2 人即可开局（民 1 + 卧底 1）。"
      },
      "min_players": {
        "description": "最低开局人数",
        "type": "int",
        "default": 2,
        "hint": "范围 2-10；低于该人数不会自动开局，手动开始也会拒绝。"
      },
      "camp_scales_default": {
        "description": "默认阵营比例（平民 卧底 白板）",
        "type": "string",
        "default": "4 1 0",
        "hint": "以空格或冒号分隔，如 4:1:0；实际会按参与人数按比例分配，保证卧底至少 1 人。"
      },
      "allow_host_customize_camp_scales": {
        "description": "允许首位玩家（房主）自定义阵营比例",
        "type": "bool",
        "default": true,
        "hint": "开启后，第一个进入谁是卧底房间玩家席的玩家可以在 WebUI 调整默认的民/卧/白比例，无需进入管理台。"
      },
      "match_seconds": {
        "description": "匹配等待时长（秒）",
        "type": "int",
        "default": 180,
        "hint": "范围 10-600；达到上限时若仍未凑满最大席位，按当前已入座人数开局。"
      },
      "prepare_seconds": {
        "description": "发词准备时长（秒）",
        "type": "int",
        "default": 10,
        "hint": "范围 0-120；开局身份卡上「确认」按钮按此时长锁定（倒计时结束后才可确认开局），期间可查看身份与词条；设为 0 则直接可确认。"
      },
      "speaking_seconds": {
        "description": "单人次发言时长上限（秒）",
        "type": "int",
        "default": 160,
        "hint": "范围 0-600，默认 160 秒；设为 0 表示不限时：后端不设超时、不强跳发言玩家，由玩家自行发言推进（前端显示「不限时」）。"
      },
      "voting_seconds": {
        "description": "投票时长上限（秒）",
        "type": "int",
        "default": 120,
        "hint": "范围 0-600，默认 120 秒；超时未投票视为本轮弃权。"
      },
      "first_round_non_voting": {
        "description": "首轮不投票的最低存活人数",
        "type": "int",
        "default": 3,
        "hint": "范围 2-10；首轮存活人数 ≤ 该值时，第一轮发言后直接进入下一轮，不再投票。"
      },
      "send_identity_in_card": {
        "description": "告知身份（开场发放身份/词条卡）",
        "type": "bool",
        "default": true,
        "hint": "开启后开场会以动画卡片告知玩家身份与词条；关闭则不展示身份卡。"
      },
      "show_voters": {
        "description": "投票结算展示具体投票人",
        "type": "bool",
        "default": false,
        "hint": "开启后，投票结果会展示每张票投给了谁（时间线与房间对话同步）；关闭则只显示各玩家得票数与被淘汰结果。"
      },
      "similarity": {
        "description": "发言相似度阈值（%）",
        "type": "int",
        "default": 80,
        "hint": "范围 0-100；发言与历史发言相似度超过该阈值会被驳回并要求换说法，防止复读；0 表示不检测。"
      },
      "ai_fill_enabled": {
        "description": "开启 AI 玩家自动补位（可作为人数不足的后备玩法）",
        "type": "bool",
        "default": true,
        "hint": "开启后，如果人数少于最低开局人数，Bot 会自动以 AI 玩家身份补位。AI 玩家会自动发言描述和投票。"
      },
      "ai_fill_min_players": {
        "description": "AI 补位后的最低总人数",
        "type": "int",
        "default": 3,
        "hint": "范围 2-8；真实玩家 + AI 玩家达到该人数才开局；真实玩家如果已≥最低开局人数，则不强制补 AI。"
      },
      "failed_mute_seconds": {
        "description": "失败方禁言时长（秒）",
        "type": "int",
        "default": 60,
        "hint": "对局失败阵营被禁言的秒数，向 60 秒向上取整；0 表示不禁言。"
      },
      "violated_mute_seconds": {
        "description": "违规说出词条禁言时长（秒）",
        "type": "int",
        "default": 300,
        "hint": "平民/卧底误说自己的词条后被禁言的秒数，向 60 秒向上取整；0 表示不禁言。"
      },
      "group_announce_enabled": {
        "description": "开启群通报",
        "type": "bool",
        "default": false,
        "hint": "开启后，结算卡上「通报到群」可用，将本局胜负方与惩罚发到开房群（一局一次）。"
      },
      "group_announce_auto": {
        "description": "自动播报",
        "type": "bool",
        "default": false,
        "hint": "开启后每局结束自动发群通报，无需手动点按钮。"
      },
      "group_announce_style": {
        "description": "群通报发送方式",
        "type": "string",
        "default": "text",
        "options": ["text", "image", "both"],
        "labels": ["仅文字", "仅图片", "文字+图片"],
        "hint": "图片为前端生成的本局战报海报；「仅图片」模式可选附带复盘时，图片前会带一行标题、不发详情；需开启群通报后才生效。"
      },
      "group_announce_recap": {
        "description": "群通报附带复盘",
        "type": "bool",
        "default": true,
        "hint": "开启后随通报附带「花火复盘」（图文模式时放在图片之后）；关闭则只发战报本身。"
      },
      "report_enabled": {
        "description": "开启战绩日报/周报",
        "type": "bool",
        "default": false,
        "hint": "开启后按下方时间/白名单定时推送谁是卧底战绩榜。"
      },
      "report_mode": {
        "description": "推送频率",
        "type": "string",
        "default": "daily",
        "options": ["daily", "weekly"],
        "labels": ["每日", "每周"],
        "hint": "weekly 时每天由 report_weekday 指定星期几推送一次。"
      },
      "report_time": {
        "description": "推送时间（HH:MM）",
        "type": "string",
        "default": "09:00",
        "hint": "本地时间；示例 09:00。周报同样取该时分。"
      },
      "report_weekday": {
        "description": "每周推送的星期（0-6，0=周一）",
        "type": "int",
        "default": 1,
        "hint": "仅 report_mode 为 weekly 时生效；与 report_time 配合。"
      },
      "report_groups": {
        "description": "推送白名单（UMO 会话串）",
        "type": "text",
        "default": "",
        "hint": "每行一个或逗号/空格分隔，例如 UMO:huahuo:GroupMessage:454366619。"
      }
    }
  },
  "memory": {
    "description": "陪伴记忆",
    "type": "object",
    "items": {
      "record_shared_experience": {
        "description": "记录确认身份后的完整游戏经历",
        "type": "bool",
        "default": true,
        "hint": "只在房间销毁时写一条摘要；不会保存逐步棋谱。"
      }
    }
  },
  "companion_integration": {
    "description": "我会永远陪着你联动",
    "type": "object",
    "hint": "需要安装支持游戏事件扩展 API 的 astrbot_plugin_private_companion；未安装或版本较旧时自动降级，不影响游戏本身。",
    "items": {
      "enable_emotional_afterglow": {
        "description": "启用游戏情绪余韵",
        "type": "bool",
        "default": false,
        "hint": "每局结束后按已绑定玩家分别上报结构化结果，由陪伴人格决定胜负在意程度、叠加上限、持续时间和后续语气。"
      },
      "enable_proactive_invites": {
        "description": "允许陪伴主动邀请游戏",
        "type": "bool",
        "default": false,
        "hint": "向陪伴插件注册可选主动能力；是否真正邀请仍由陪伴人格、日程、关系、免打扰和主动消息额度共同决定。"
      },
      "proactive_invite_probability_percent": {
        "description": "主动邀请候选权重百分比",
        "type": "int",
        "default": 18,
        "hint": "范围 0-100，仅影响该能力进入主动动作选择时的相对权重，不代表固定发送概率。"
      },
      "proactive_invite_cooldown_hours": {
        "description": "主动邀请冷却小时",
        "type": "int",
        "default": 24,
        "hint": "由陪伴插件统一执行冷却；0 表示不增加该能力自己的额外冷却。"
      }
    }
  }
}
